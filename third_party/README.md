# Third-party Dependencies

This directory is reserved for local development dependencies.

Planned dependencies:

- librime x64
- Rime shared data

Do not commit downloaded binary packages directly unless the licensing and
release process explicitly allow it.
